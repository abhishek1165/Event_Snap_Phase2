## Confirmed — ready to build (Tailwind skill now incorporated)

Yes, I've now read `tailwind-design-system`. It targets **Tailwind v4**, but this project is **v3.4.17**, so I will NOT migrate (avoids risk to the 46 shadcn components). Instead I apply its principles the v3 way:

- **Semantic CSS-var tokens** in `index.css` + `theme.extend` in `tailwind.config.js` → app pages use `bg-surface`/`text-accent` instead of hardcoded hex
- **CVA** shared atoms (cva already installed via shadcn) with `focusRing`/`disabled`/`aria-invalid`/`role="alert"` patterns
- **Compound components** (Card family) + **Container/Grid** → kills hand-rolled per-page headers
- App pages get lifted onto the same token *values* the landing already uses

Everything else stands as the approved plan: backend untouched, Event Snap brand, dark-only, all 7 pages, `@gsap/react` added, GSAP-for-marketing + Framer-Motion-for-app-UI convention.

**No new questions.** Proceeding to build in this order:
1. Foundation (tokens/index.css, Logo, AppShell, cva atoms, hooks, tailwind config, install `@gsap/react`)
2. Landing rebrand + elevated hero
3. Auth
4. Dashboard + EventDetails
5. AttendeeEntry + SelfieCapture + PhotoGallery
6. Polish (focus, a11y, responsive, reduced-motion) → `build` → route walkthrough → `git status backend/`